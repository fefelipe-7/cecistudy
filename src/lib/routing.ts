// Compat stub — a lógica pura de rota hash ↔ pilha vive em `packages/navigation`.
// Re-exporta daqui (caminho relativo, ver AGENTS.md) mantendo todos os imports existentes.
export * from '../../packages/navigation/src/hash';

/**
 * Compat re-export → `@cecistudy/contracts` (canonical JSON v1).
 * Lógica de serialização canônica em `packages/contracts/src/canonical-json.ts`;
 * spec em `cecistudy-rust/docs/canonical-json-v1.md`.
 */
export * from '../../packages/contracts/src/canonical-json';
/** Normalização compartilhada com a pipeline editorial (espelho de content/temple-lib.mjs). */
export function normalize(s: string): string {
  return String(s ?? '')
    .toLowerCase()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/[^a-z0-9]+/g, ' ')
    .trim();
}

// Compat stub — a lógica pura da pilha do flush de quiz vive em `packages/navigation`.
// Re-exporta daqui (caminho relativo, ver AGENTS.md) mantendo todos os imports existentes.
export * from '../../packages/navigation/src/stack';

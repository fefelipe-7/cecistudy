{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "permissões padrão do cecistudy desktop (janela principal)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "updater:default",
    "process:allow-restart",
    "window-state:default"
  ]
}
